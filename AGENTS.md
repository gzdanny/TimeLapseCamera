# Agent 铁律：修改工作流

每次用户提出功能修改要求（无论大小），必须严格遵守以下三轮对话流程，**不得跳过任何步骤**。

---

## 第一轮：计划确认

1. 将修改方案讲清楚：改哪些文件、改什么内容、改动逻辑
2. 对计划要用的API必须上https://developer.android.com/核查清楚，不要自己幻想一个API和最佳实践
3. **必须等用户明确确认后**，才能进入第二轮
4. 禁止跳过计划直接写代码

## 第二轮：修改执行 + 等待审计

1. 按确认的方案执行修改
2. 修改完成后，告知用户等待审计
3. **禁止 commit、禁止 push、禁止编译**
4. 用户审计后：
   - 如果发现问题 → **回到第一轮**，重新讲计划、确认后再改
   - 如果用户说"通过审计" → commit（禁止push），commit message 写清楚修改内容，不需要版本号

## 第三轮：等待新指令

1. 用户提出下一步修改计划 → 回到第一轮
2. 循环往复

---

## 阶段结束（Push 触发条件）

只有当用户明确说"**本轮功能完成，版本号调整为xxx**"时，才执行以下操作：

1. 同步更新 `app/build.gradle` 版本号
2. 同步更新设置界面中的版本号显示
3. commit message 格式：
   - 第一行：版本号（如 `v0.9.5`）
   - 后续行：总结上次 push 以来累积的修改内容
4. commit（禁止push）

---

## 禁止事项

- **禁止跳过第一轮直接写代码**
- **禁止未经用户确认就执行修改**
- **禁止在用户未说"通过审计"时 commit**
- **禁止push**
- **禁止编译**
